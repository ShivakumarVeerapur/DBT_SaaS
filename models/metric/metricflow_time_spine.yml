version: 2
models:
  - name: metricflow_time_spine
    description: "Time spine model for MetricFlow semantic layer."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: "Standard granularity date column."
        data_type: date
        granularity: day
